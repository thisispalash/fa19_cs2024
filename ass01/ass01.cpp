#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
  cout << "I will be using the command line on macOS (so clang mostly).\n";
  cout << "I just missed working with cpp, hence taking this course.\n";
  return 0;
}